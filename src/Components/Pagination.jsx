import React from 'react'

const Pagination=(props)=> {
  return (
   <nav>
       <ul className="pagination">
        <li className="page-item">
            
        </li>
       </ul>
   </nav>
  )
}

default Pagination
